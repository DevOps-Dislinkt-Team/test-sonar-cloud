# Set the language
language: node_js

# Set the Node version
node_js:
  - '14.17.4'

services:
  # Use Docker command line
  # - docker

install:
  # Install dependencies for tests
  # - npm ci

addons:
  sonarcloud:
    organization: 'devops-dislinkt-team'
    token:
      secure: cd5e3b11dc94c3480510416c4f6bba2536b217f4

before_script:
  # Start server and client for tests
  # - npm run dev &

script:
  # Run tests
  # - npm run test
  - sonar-scanner -X

branches:
  only:
    - main
    - develop
